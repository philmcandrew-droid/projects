apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: hello
  labels:
    app: hello
spec:
  to:
    kind: Service
    name: hello
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
