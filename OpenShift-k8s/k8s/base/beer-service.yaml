apiVersion: v1
kind: Service
metadata:
  name: beer
  labels:
    app: beer
spec:
  type: ClusterIP
  selector:
    app: beer
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
